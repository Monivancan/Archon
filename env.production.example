# Archon Production Environment Configuration for Coolify
# This template is for deploying Archon to Coolify with separate subdomains

# ===========================================
# REQUIRED: Supabase Configuration
# ===========================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# ===========================================
# Service Ports (Keep as defaults)
# ===========================================
ARCHON_SERVER_PORT=8181
ARCHON_MCP_PORT=8051
ARCHON_AGENTS_PORT=8052
ARCHON_UI_PORT=3737

# ===========================================
# Production Configuration
# ===========================================
SERVICE_DISCOVERY_MODE=production
LOG_LEVEL=INFO
PROD=true

# ===========================================
# Production Service URLs
# ===========================================
# Update these with your actual domain
ARCHON_SERVER_URL=https://api.yourdomain.com
ARCHON_MCP_URL=https://mcp.yourdomain.com
ARCHON_AGENTS_URL=https://agents.yourdomain.com
ARCHON_FRONTEND_URL=https://archon.yourdomain.com

# Frontend API URL (same as ARCHON_SERVER_URL)
VITE_API_URL=https://api.yourdomain.com

# ===========================================
# CORS Security
# ===========================================
# Comma-separated list of allowed origins
CORS_ORIGINS=https://archon.yourdomain.com

# ===========================================
# Features
# ===========================================
AGENTS_ENABLED=true

# ===========================================
# Optional: AI Provider API Keys
# ===========================================
# Add your API keys here - at least one is recommended
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# ===========================================
# Optional: Observability
# ===========================================
LOGFIRE_TOKEN=

# ===========================================
# Coolify Deployment Notes:
# ===========================================
# 1. Use docker-compose.prod.yml for deployment
# 2. Configure these environment variables in Coolify's environment section
# 3. Set up the following subdomains in Coolify:
#    - archon.yourdomain.com → archon-frontend (port 80)
#    - api.yourdomain.com → archon-server (port 8181)
#    - mcp.yourdomain.com → archon-mcp (port 8051)
#    - agents.yourdomain.com → archon-agents (port 8052)
# 4. Ensure SSL/TLS certificates are configured for all subdomains
# 5. Database setup: Run migration/complete_setup.sql in your Supabase SQL editor

